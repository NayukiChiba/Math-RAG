# runner.py

## 概述
`modelEvaluation/retrievalEval/runner.py` 作为该组单纯评价不同类型的或者配置打分引擎（不含大模型，仅包含基于比如字面混并打分策略好坏判定）的总流程和状态控制器。它将通过提取配置后调度多个测试 `workers` 或者分批并发向待测寻找引擎发生询问调用，接回其产生所有的结果序列表推送给核心计算机制和处理中断超时异常并写入结果大包和最后通知调用下一级打点工具组件控制业务结构。