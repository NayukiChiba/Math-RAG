# retrievers.py

## 概述
`modelEvaluation/retrievalEval/retrievers.py` 这个可能是只向该评测核心内特定的测试模块，例如模拟的打分模型提供测试假对象用的包裹，又或者这实际上是在大流水线上将来自系统本身的核心 `retrieval` 封装进行二次配置注入，为了特定在评价模式中运行比如开启特殊的收集打印，跳掉某个缓存模块并注入测试追踪变量钩子的一种特殊的针对于被评估系统所做的打桩、测试接线端特供模型环境启动组装包装库。