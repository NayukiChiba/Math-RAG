# addMissingTerms.py

## 概述
`scripts/tools/addMissingTerms.py` 这个小脚本工具可能是为了从报错日志或跑偏了的回溯文档集中自动或半自动地拾取出那些没有被我们的 `synonyms` 或者倒排字典识别到的非常偏僻的物理量和各种缩略符号等生僻概念然后强行通过工具结构将其打进我们的词组扩展或者是近义词补丁包里面提供运营维护字典功能的独立运维工作组件层包裹体。