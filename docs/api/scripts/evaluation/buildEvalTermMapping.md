# buildEvalTermMapping.py

## 概述
`scripts/evaluation/buildEvalTermMapping.py` 这是一种往往在开始大规模机器评分之前将那些非常冷僻但是由于扫描仪或是不同人手写带来的同概念但不同表示字符的数学等价概念字典通过一次大模型的聚类，转换成能够支持打分模型或是 `Recall` 工具在面对比如“均值”、“期望”这类词在比对时知道是匹配的预处理准备阶段所生成的同义与子集关系图/特征项对齐图。这个脚手架是为了评估阶段对齐而使用离线缓存计算从而减少反复计算等价矩阵的功能组件。