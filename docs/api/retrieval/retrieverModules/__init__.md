# __init__.py

## 概述
`retrieval/retrieverModules/__init__.py` 是检索核心引擎各个底层具体算法包暴露对象的总调度组装声明入口。其功能是将散落在如基于向量的（`vector`），基于倒排打分的（`bm25`）等各种多路召回搜索的单独代码块执行门面聚拢和提取暴露出诸如 `get_retriever(mode)` 等提供给业务生命管线的工厂模式包装获取调用拦截组件标识位置等实现集合。
