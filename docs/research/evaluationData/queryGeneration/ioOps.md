# ioOps.py

## 概述
`evaluationData/queryGeneration/ioOps.py` 是为了这部分造数据测试题专用提供的一个隔离出的针对文件或标准流输入输出控制句柄封装点工具集代码块。用于解决把生成的 QA 对或者是问题及对应的标注真实来源引用文件节点信息按照某些统一且特定的结构（如 `.jsonl` 等流式大文件可承载格式）在磁盘存储区安全的落地、读取或者验证完整性。