# cli.py

## 概述
`evaluationData/queryGeneration/cli.py` 提供关于批量造测试问题脚本程序的外部暴露的终端命令入口支持模块层。通过利用如 `argparse` 或 `Click` 挂载接受用户指定的源文本段落文件夹位置，想要生成的期望测试对数目，调用的后端生成用的大模型代理以及输出测试集的序列化位置，把输入转化并传发给后续 `runner` 等调用配置转换接口层。