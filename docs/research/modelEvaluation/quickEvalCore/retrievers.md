# retrievers.py

## 概述
`modelEvaluation/quickEvalCore/retrievers.py` 这个主要是提供那种用于非常小规模或者甚至是向纯缓存进行直接调用的一个轻量仿制获取器，以跳过真实系统需要重加载所有的复杂例如带有 `FAISS` 热更或者是启动时间极长的高耗时依赖节点，专门针对于这种仅仅是为了在不通真实庞大组件验证主干正确或者部分逻辑修改时候的小型装载与特制分发获取类的核心封包对象位置控制引擎环境测试实现方法。