# constants.py

## 概述
`modelEvaluation/quickEvalCore/constants.py` 提供向仅作为部分或者简陋环境抽样的小测试在运行中不用反复传递或者计算需要直接调用的魔法数字（像特定的阈差，诸如默认仅仅选取二十条样等，或者是写死的测试用例极短样例存放地址点等各种轻量测试时写在统一地点便于管理且不影响大框架运行的系统配置及环境映射控制点方法配置表结构管理引擎。