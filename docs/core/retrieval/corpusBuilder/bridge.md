# bridge.py

## 概述
`retrieval/corpusBuilder/bridge.py` 它是对外部像类似于 `LlamaIndex` 或者是其他的标准图数据库等格式与内部特化的、自己项目自己用的对象之间架设的一个格式转换/桥接转换器模块代码。由于原生的库常常没有关于复杂的数学相关属性元数据的承载结构或者是层级过重，或者想要更换下层的驱动数据库引擎时。它起到了解耦以及对脏数据提供一个适配层转换的业务实现包裹体。