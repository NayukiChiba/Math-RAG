# io.py

## 概述
`retrieval/corpusBuilder/io.py` 用来从操作系统读取或网络拉去未格式化的原始电子源（如 txt，PDF解析结果，或者之前 OCR 后沉淀下来的初步 JSONL 数据）的提供载体端点封装支持包；或是将最后由 `builder` 制作好的结构树、向量映射实体或是压缩格式元数组往服务器上和硬盘特定路径作安全的写入/替换更新/读取（例如初始化加载语料库环境文件时的读取，以及热更时候覆盖锁和读写版本保护操作机制）。